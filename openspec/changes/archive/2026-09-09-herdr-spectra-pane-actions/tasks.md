## 1. Pure ordering and filtering functions over the snapshot

- [x] 1.1 Implement `src/tui/change-order.ts` to sort changes within groups: `SORT_MODES` cycle order modified → name → created, `nextSortMode`, and `compareChanges(mode)` with dates descending, unknown dates last, ties by name ascending. Verified by `test/tui/change-order.test.ts` asserting the three-change example (Name, Modified, Created orders), the unknown-date-last rule for both date modes, and the cycle order.
- [x] 1.2 Implement `src/tui/change-filter.ts` to filter changes by name (`matchesName`, case-insensitive substring on the name only) and to filter changes by author (`authorCandidates` with case-insensitive ordering and trailing `Unknown`, `matchesAuthors` with the `__unknown__` sentinel, `filterSnapshot` returning per-group totals and a `filtered` flag combining both predicates as a conjunction). Verified by `test/tui/change-filter.test.ts` asserting every row of the filter-text/author combination table and the candidate-list table, plus that a proposer string is not matched by the name filter.

## 2. Tree model and rendering

- [x] 2.1 Extend `src/tui/tree-model.ts` and `src/tui/ChangeTree.tsx` so the pane continues to display changes as a grouped tree with `(<count>)` labels when no filter is active and `(<matching>/<total>)` when one is, and so change rows carry `proposer` and `progressText` fields rendered as the proposer rendered inside the change label with `dimColor`, keeping counts last. This covers show the proposer on change nodes and the modified show task progress on change nodes rule. Verified by `test/tui/App.test.tsx` cases asserting `Active (1/3)` under a filter and every row of the node-text-by-proposer-and-progress table.
- [x] 2.2 Add the header line shows sort mode and active filters in `src/tui/App.tsx`: `sort: <mode>` always, `filter: <text>` and `authors: <labels>` when active, rendered as the input line `/ <text>▌` in filter mode; raise `RESERVED_ROWS` to 5 so the tree window still fits. Verified by `test/tui/App.test.tsx` cases asserting the header text in each state and that the tall-tree scrolling test still passes.

## 3. Input modes and keys

- [x] 3.1 Introduce the three input modes: tree, filter line, author picker in `src/tui/App.tsx` (`mode` state dispatched first in `useInput`) so that the pane continues to provide a Spectra changes pane where `q`/Escape exit only in tree mode. Add `s` to cycle sort mode and re-sort without rescanning, with the status bar hint updated in `src/tui/StatusBar.tsx` and `src/tui/keymap.ts`. Verified by `test/tui/App.test.tsx` cases: `s` reorders the example changes and updates the header, scan called once; typing `q` in filter mode appends to the text and does not call `onExit`.
- [x] 3.2 Implement filter mode (`/` enters, printable appends, Backspace deletes, Enter keeps and returns, Escape clears and returns, re-filter on each keystroke) in `src/tui/App.tsx` and `src/tui/FilterLine.tsx`, satisfying filter changes by name. Verified by `test/tui/App.test.tsx` cases: `/search` Enter narrows Active and Parked and shows `filter: search`; `SEARCH` matches `add-search`; artifacts of a matching change remain listed; a no-match text shows `(0/N)` groups with no children; Escape clears; Backspace edits `sea` to `se`.
- [x] 3.3 Implement authors mode (`@` opens `src/tui/AuthorPicker.tsx` listing `[x]`/`[ ]` candidates; j/k and arrows move, Space toggles, Enter/Escape close keeping the selection; fewer than two candidates shows `No authors to filter by` and stays in tree mode), satisfying filter changes by author with the header `authors: <labels>`. Verified by `test/tui/App.test.tsx` cases: selecting `alice` shows only her change and the header; selecting `Unknown` shows only the unknown-proposer change; conjunction with filter text `add` and author `alice` shows `add-dark-mode` only; single-candidate snapshot refuses to open.
- [x] 3.4 Implement the `y` key so copy name reuses the clipboard adapter and the status bar: copy the change name to the clipboard for change and artifact nodes with `Copied: <name>` / `Copy failed: <name>`, and `Select a change first` on group nodes, with no rescan. Verified by `test/tui/App.test.tsx` cases asserting the clipboard text for a change node (`sort-and-filter-changes` with proposer and progress present), for an artifact node (`add-search`), the group-node message, and `scan` called once.
- [x] 3.5 Make rescan on demand preserve sort mode and filter text and apply the rule that author selection survives rescan by intersection with the new candidate list. Verified by `test/tui/App.test.tsx` cases: after `R` with sort Name and filter `search` the new snapshot is ordered by name and filtered; after `R` with `alice` and `bob` selected and `bob` absent, only `alice` remains selected and the header shows `authors: alice`.

## 4. Acceptance

- [x] 4.1 Run `npm test`, `npm run typecheck`, and `npm run check-dist`; all exit 0 and the new test files `test/tui/change-order.test.ts` and `test/tui/change-filter.test.ts` exist. Verified by the command output recorded in the acceptance notes.
- [x] 4.2 Manual acceptance in a live Herdr session: open the pane in this repository, press `s` twice and confirm the header cycles to `sort: created`, press `/core` Enter and confirm `Active (1/1)` style counts and `filter: core`, press `y` on the change and confirm the clipboard holds its name. Verified by a checklist with observed results appended to `openspec/notes/`.
